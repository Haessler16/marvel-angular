import { SearchCreatorsPipe } from './search-creators.pipe';

describe('SearchCreatorsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCreatorsPipe();
    expect(pipe).toBeTruthy();
  });
});
